server:
  port: 8081
  shutdown: graceful

spring:
  application:
    name: room-manager
  config:
    import: optional:configserver:http://config-service:8888

management:
  endpoints:
    web:
      exposure:
        include: health,info
  health:
    circuitbreakers:
      enabled: true

spring.lifecycle:
  timeout-per-shutdown-phase: 20s

resilience4j:
  circuitbreaker:
    instances:
      roomManager:
        sliding-window-size: 10
        failure-rate-threshold: 30
        wait-duration-in-open-state: 10s
        permitted-number-of-calls-in-half-open-state: 3
        automatic-transition-from-open-to-half-open-enabled: true
<<<<<<< HEAD
  
=======

management:
  health:
    circuitbreakers:
      enabled: true
>>>>>>> 889e01a (Initial commit - Room Reservation System with event chain)
  endpoints:
    web:
      exposure:
        include: health, circuitbreakers
